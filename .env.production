# Formspree form ID for the Contact section.
# Form IDs are *not* secrets — they're embedded in the built JS bundle
# and are how Formspree identifies which form to post into. Spam and abuse
# are handled server-side by Formspree (CAPTCHA, domain allowlist, etc.).
VITE_FORMSPREE_ID=xdaybndv
